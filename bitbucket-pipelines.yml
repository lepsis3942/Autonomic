pipelines:
  branches:
    master:
      - step:
          name: Test
          image: bitbucketpipelines/android-ci-image
          caches:
            - gradle
          script:
            - chmod +x gradlew
            - ./gradlew test
