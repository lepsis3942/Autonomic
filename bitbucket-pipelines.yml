pipelines:
  branches:
    master:
      - step:
          name: Test
          image: bitbucketpipelines/android-ci-image
          caches:
            - gradle
          script:
            - chmod +x setup_licenses.sh
            - ./setup_licenses.sh
            - chmod +x gradlew
            - ./gradlew test
