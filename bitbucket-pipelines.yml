pipelines:
  branches:
    master:
      - step:
          name: Test
          image: bitbucketpipelines/android-ci-image
          caches:
            - gradle
          script:
            - ls -la $ANDROID_HOME
            - yes | $ANDROID_HOME/sdkmanager --licenses
            - chmod +x gradlew
            - ./gradlew test
